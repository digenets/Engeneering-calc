#include "rpn_utilities.h"

/* bool isPow (char* token){
    char tmp_token[3] = {0};
    for (int i = 0; i < 3; i++){
        tmp_token[i] = token[i];
    }
    if (strcmp(tmp_token, "pow") == 0)
        return true;
    else return false;
} */